<p id="notice"><%= notice %></p>

<%= react_component 'Company', @customer.values
    .merge(device_types: @device_types)
    .merge(actions: @actions)
    .merge(map_link: @customer.map_link)
    .merge(assigned: [{id: nil, name: "Next Available Tech"}] + Employee.order(:name).map{|e| {id: e.id, name: e.name}})
    .merge(@employee_id)%>

<%= link_to 'Back', customers_path %>
